import {NextRequest} from "next/server";import {connectDB} from "@/lib/db";import Attendance from "@/models/Attendance";import {getSession,requireRole} from "@/lib/auth";import {attendanceCreate,bulkAttendance} from "@/validation";import {ok,error,handleError} from "@/lib/http";import {pagination} from "@/lib/query";
export const runtime="nodejs";
export async function GET(req:NextRequest){try{const s=await getSession(req),d=requireRole(s,["CR","STUDENT","ADMIN"]);if(d)return d;await connectDB();const sp=req.nextUrl.searchParams,p=pagination(sp),f:any={};for(const k of ["student","subject","status"])if(sp.get(k))f[k]=sp.get(k);if(sp.get("from")||sp.get("to"))f.date={};if(sp.get("from"))f.date.$gte=new Date(sp.get("from")!);if(sp.get("to"))f.date.$lte=new Date(sp.get("to")!);if(s!.role==="STUDENT")f.student=(await (await import("@/models/User")).default.findById(s!.userId)).studentId;const [items,total]=await Promise.all([Attendance.find(f).populate("student subject markedBy","name rollNumber code email").sort({date:-1}).skip(p.skip).limit(p.limit),Attendance.countDocuments(f)]);return ok({items,total,page:p.page,limit:p.limit})}catch(e){return handleError(e)}}
export async function POST(req:NextRequest){try{const s=await getSession(req),d=requireRole(s,["CR","ADMIN"]);if(d)return d;await connectDB();const b=attendanceCreate.parse(await req.json());const x=await Attendance.findOneAndUpdate({student:b.student,subject:b.subject,date:b.date},{...b,markedBy:s!.userId},{upsert:true,new:true,setDefaultsOnInsert:true});return ok(x,201)}catch(e){return handleError(e)}}