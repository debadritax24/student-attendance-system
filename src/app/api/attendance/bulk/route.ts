import {NextRequest} from "next/server";import {connectDB} from "@/lib/db";import Attendance from "@/models/Attendance";import {getSession,requireRole} from "@/lib/auth";import {bulkAttendance} from "@/validation";import {ok,handleError} from "@/lib/http";
export const runtime="nodejs";
export async function POST(req:NextRequest){try{const s=await getSession(req),d=requireRole(s,["CR","ADMIN"]);if(d)return d;await connectDB();const b=bulkAttendance.parse(await req.json());const ops=b.records.map(r=>({updateOne:{filter:{student:r.student,subject:b.subject,date:b.date},update:{$set:{...r,subject:b.subject,date:b.date,markedBy:s!.userId}},upsert:true}}));const result=await Attendance.bulkWrite(ops);return ok({matched:result.matchedCount,modified:result.modifiedCount,upserted:result.upsertedCount},201)}catch(e){return handleError(e)}}