import {NextRequest} from "next/server";import {connectDB} from "@/lib/db";import Subject from "@/models/Subject";import {getSession,requireRole} from "@/lib/auth";import {subjectCreate} from "@/validation";import {ok,error,handleError} from "@/lib/http";
export const runtime="nodejs";
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["CR","STUDENT","ADMIN"]);if(d)return d;await connectDB();const x=await Subject.findById((await params).id);return x?ok(x):error("Subject not found",404)}catch(e){return handleError(e)}}
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["CR","ADMIN"]);if(d)return d;await connectDB();const x=await Subject.findByIdAndUpdate((await params).id,subjectCreate.partial().parse(await req.json()),{new:true,runValidators:true});return x?ok(x):error("Subject not found",404)}catch(e){return handleError(e)}}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["ADMIN"]);if(d)return d;await connectDB();const x=await Subject.findByIdAndDelete((await params).id);return x?ok({deleted:true}):error("Subject not found",404)}catch(e){return handleError(e)}}