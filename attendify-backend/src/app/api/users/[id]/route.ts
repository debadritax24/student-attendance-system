import { NextRequest } from "next/server";
import bcrypt from "bcryptjs"; import { connectDB } from "@/lib/db"; import User from "@/models/User"; import { getSession,requireRole } from "@/lib/auth"; import { ok,error,handleError } from "@/lib/http"; import { z } from "zod";
export const runtime="nodejs"; const body=z.object({name:z.string().min(2).optional(),email:z.string().email().optional(),password:z.string().min(6).optional(),role:z.enum(["CR","STUDENT","ADMIN"]).optional(),studentId:z.string().nullable().optional()});
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["ADMIN"]);if(d)return d;await connectDB();const u=await User.findById((await params).id).select("-passwordHash");if(!u)return error("User not found",404);return ok(u)}catch(e){return handleError(e)}}
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["ADMIN"]);if(d)return d;const b=body.parse(await req.json());await connectDB();if(b.password)b.password=await bcrypt.hash(b.password,12);const {$unset,...rest}=b as any;const u=await User.findByIdAndUpdate((await params).id,rest,{new:true,runValidators:true}).select("-passwordHash");if(!u)return error("User not found",404);return ok(u)}catch(e){return handleError(e)}}
export async function DELETE(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const s=await getSession(req),d=requireRole(s,["ADMIN"]);if(d)return d;await connectDB();const u=await User.findByIdAndDelete((await params).id);if(!u)return error("User not found",404);return ok({deleted:true})}catch(e){return handleError(e)}}